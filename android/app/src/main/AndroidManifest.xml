<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.OjoGPS">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recibe la ubicación mandada desde la PC por
             "adb shell am broadcast" (ver ojo_gps_android_bridge.py). -->
        <receiver
            android:name=".LocationCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.ojogps.android.SET_LOCATION" />
                <action android:name="app.ojogps.android.STOP" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
