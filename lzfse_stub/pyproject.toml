[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lzfse"
version = "0.4.2"
description = "Ojo GPS compatibility shim for Windows Python 3.13"
requires-python = ">=3.13"
